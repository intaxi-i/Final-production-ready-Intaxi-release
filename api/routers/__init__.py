"""FastAPI router modules for the Intaxi API.

Routes are split by product area so production endpoints are defined in
explicit router files instead of being hidden in a single monolithic module
or runtime patch layer.
"""
